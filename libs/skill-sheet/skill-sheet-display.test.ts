import { describe, it } from "vitest";

/**
 * 経歴の表示用整形が libs に分離される場合の振る舞い仕様。
 * コンポーネント内に閉じる場合は UI テストへ寄せ、本ファイルは削除してよい。
 */
describe("formatSkillCareerLine（仮称）", () => {
  // モーダル・一覧で用いる1行表記の契約を固定する
  it.todo(
    "期間とプロジェクト名と概要が与えられたとき、所定の区切り文字で1行にまとまること"
  );

  // 概要が無い場合の表記ゆれを防ぐ
  it.todo("概要が空のとき、表示から除外されること");
});
