import { describe, it } from "vitest";

/**
 * プロフィール編集とスキルシート編集のタブ切替・共通保存・経歴モーダル・動的行。
 * 実装コンポーネント名に合わせてファイル名・describe をリネームしてよい。
 * 本文は it.todo のみ。
 */
describe("ProfileEditorTabs（仮称）", () => {
  describe("タブ切替", () => {
    // スキルシート編集 UI の表示切替
    it.todo(
      "スキルシートタブを選んだとき、スキルシート用フィールドが表示されること"
    );

    // プロフィール編集 UI の表示切替
    it.todo(
      "プロフィールタブを選んだとき、プロフィール用フィールドが表示されること"
    );

    // 単一 form 前提で入力が保持されること
    it.todo(
      "タブを往復しても、編集中の入力値が失われないこと"
    );
  });

  describe("共通の保存ボタン", () => {
    // 画面上に保存が1つだけあること
    it.todo(
      "どちらのタブを表示中でも、同じラベルの保存ボタンが1つだけ存在すること"
    );

    // 片方タブからでもプロフィール＋スキルシートの値が欠けず送信されること
    it.todo(
      "スキルシートタブ表示中に保存を押したとき、プロフィールとスキルシートの入力がまとめて送信対象になること"
    );
  });

  describe("経歴表示モーダル（プロフィール表示画面）", () => {
    // 開閉の基本動作
    it.todo(
      "経歴表示ボタンが存在し、押下でモーダルが開くこと"
    );

    // 保存済みデータの閲覧
    it.todo(
      "モーダルが開いているとき、保存済みの経歴内容が読めること"
    );

    // 閉じる操作は実装に合わせる（×／オーバーレイ／閉じるボタン等）
    it.todo(
      "閉じる操作でモーダルが閉じること"
    );

    // データが無い場合の空状態
    it.todo(
      "経歴が空のとき、空であることがユーザーに伝わる表示であること"
    );
  });

  describe("経歴行の追加（一行ずつ追加）", () => {
    it.todo(
      "行を追加すると、新しい入力欄のセットが1行分増えること"
    );

    it.todo(
      "複数回追加すると、行数がその回数だけ増えること"
    );

    it.todo(
      "各行に独立して入力でき、保存に反映されること"
    );
  });

  describe("アクセシビリティ", () => {
    // ラベル関連付け（プルダウン）
    it.todo(
      "経験年数プルダウンにアクセシブル名が関連付けられていること"
    );

    // フォーカス管理は実装した場合のみ検証する
    it.todo(
      "モーダルが開いたとき、フォーカスがモーダル内に移ること"
    );
  });
});
