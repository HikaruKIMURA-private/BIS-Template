import { describe, it } from "vitest";

/**
 * プロフィールとスキルシートを単一 Server Action でまとめて保存する場合の結合仕様。
 * 実装が profile に統合される場合は describe を移し替えてよい。
 * 本文は it.todo のみ。
 */
describe("submitProfileAndSkillSheet（仮称）", () => {
  // 認証なしでは永続化しない
  it.todo("セッションが無いとき、未認証として失敗すること");

  // 片方のバリデーション失敗で全体を止める（一括送信の契約）
  it.todo(
    "プロフィール側のバリデーションが失敗するとき、スキルシートが正しくても保存されないこと"
  );

  it.todo(
    "スキルシート側のバリデーションが失敗するとき、プロフィールが正しくても保存されないこと"
  );

  // 両方有効時のみ一括で永続化される
  it.todo(
    "プロフィールとスキルシートの両方が有効なとき、一括で永続化されること"
  );
});
